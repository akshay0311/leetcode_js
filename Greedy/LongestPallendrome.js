/**
 * @param {string} s
 * @return {number}
 */
let s = "abccccdd"
var longestPalindrome = function(s) {
    if (s.length % 2 === 1) {
        for (let i = 0; i < s.length; i++) {
            
        }
    }
    else {

    }
}

longestPalindrome(s);